{
  "name": "hogye-bus-alert",
  "version": "1.9.0",
  "private": true,
  "description": "목적지 도보권 노선과 안정적인 위치기반 주변정류소 조회를 지원하는 버스 알림 PWA",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check app.js && node --check api/gbis.js && node --check api/config.js && node --check api/firebase-messaging-sw.js && node --check functions/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
